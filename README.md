# 🧹 Auto Data Merging + Cleaning + Reporting Notebook

## 📘 Overview
This Jupyter notebook automatically merges multiple CSV/Excel files, cleans the data (missing values, duplicates), and generates summary reports with visualizations.

## 🚀 Features
- Merge multiple CSV/Excel files automatically  
- Clean missing and duplicate values  
- Generate summary reports (counts, sums, averages)  
- Create simple visualizations using Matplotlib  
- Save all outputs to the "output" folder  

## 📂 Folder Structure
